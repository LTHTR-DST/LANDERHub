# https://github.com/jupyterhub/zero-to-jupyterhub-k8s/issues/1014
proxy:
# Offload SSL at loadbalancer/nginx
  https:
    enabled: true
    type: offload
    #   - ai.xlthtr.nhs.uk
    # type: secret
    # secret:
  service:
    type: ClusterIP
    # loadBalancerIP: 172.18.16.190
    # annotations: 
    #   service.beta.kubernetes.io/azure-load-balancer-internal: "true"
